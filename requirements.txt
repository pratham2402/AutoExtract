PyYAML>=6.0

# optional: event-driven monitoring (polling fallback available)
watchdog>=6.0.0

# optional: RAR extraction (requires system unrar)
rarfile>=4.1

# optional: 7z, ISO, CAB extraction (requires system p7zip-full)
py7zr>=0.21.0

# optional: move to trash instead of permanent delete
send2trash>=1.8.0

# optional: webhook notifications
requests>=2.31.0

# optional: interactive setup wizard
rich>=13.0.0
questionary>=2.0.0
